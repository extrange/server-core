[ip]
disable=True

[fs]
show=/etc/glances.conf,/etc/resolv.conf

[diskio]
hide=nvme0n1p.*,sd[a-z]\d

[network]
hide=lo

[processlist]
disable=True

[processcount]
disable=True

[sensors]
# Note: `drivetemp` kernel module must be loaded (e.g. via /etc/modules-load.d/)
show=drivetemp.*
