services:
  open-webui:
    image: ghcr.io/open-webui/open-webui:cuda@sha256:19d67779c48057ccfc8f133ec9585995e1e1c200498ed3444706df2961f2c7a9
    container_name: open-webui
    restart: always
    volumes:
      - ./data/open-webui:/app/backend/data
    environment:
      CUSTOM_NAME: AI Chat
      ENABLE_OLLAMA_API: false
      WEBUI_SECRET_KEY: ${WEBUI_SECRET_KEY}
      USER_AGENT: Mozilla/5.0 (X11; Linux x86_64; rv:153.0) Gecko/20100101 Firefox/153.0 # For fetch_url tool (affects the default loader only)
    networks:
      - open-webui
    devices:
      - nvidia.com/gpu=all
    labels:
      traefik.enable: true
      traefik.docker.network: core_open-webui
      homepage.group: Services
      homepage.name: Open WebUI
      homepage.icon: open-webui
      homepage.href: &url https://open-webui.nicholaslyz.com
      homepage.siteMonitor: *url

  # paddleocr-vl-api:
  #   image: ccr-2vdh3abv-pub.cnc.bj.baidubce.com/paddlepaddle/paddleocr-vl:latest-nvidia-gpu
  #   command: >
  #     paddlex --serve
  #     --pipeline PaddleOCR-VL-1.6
  #     --host 0.0.0.0
  #     --port 8080
  #   networks:
  #     - open-webui
  #   devices:
  #     - nvidia.com/gpu=all
  #   restart: unless-stopped

  # Wait until API support in open-webui for V4 is released
  # mineru-api:
  #   build: .
  #   container_name: mineru-api
  #   restart: always
  #   environment:
  #     MINERU_MODEL_SOURCE: local
  #     MINERU_API_MAX_CONCURRENT_REQUESTS: 1
  #     MINERU_PROCESSING_WINDOW_SIZE: 32
  #   networks:
  #     - open-webui
  #   entrypoint: mineru-kit
  #   command:
  #     - api-server
  #     - --host
  #     - 0.0.0.0
  #     - --port
  #     - "8000"
  #   ulimits:
  #     memlock: -1
  #     stack: 67108864
  #   ipc: host
  #   healthcheck:
  #     test: ["CMD-SHELL", "curl -f http://localhost:8000/health || exit 1"]
  #   devices:
  #     - nvidia.com/gpu=all

networks:
  open-webui:
